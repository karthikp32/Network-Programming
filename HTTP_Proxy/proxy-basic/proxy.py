#Specification:  Write a program which accepts a single incoming TCP connection, 
#forwards a single message to an upstream server (such as python3 -m http.server), 
#listens for a response, then forwards back the response to the client, before closing the connection

def forward_message_to_server(dest_host, dest_port):
    pass

def main():
    proxy_host = '0.0.0.0'
    proxy_port = 8000
    server_host = 'localhost'
    server_port = 9000
    pass
